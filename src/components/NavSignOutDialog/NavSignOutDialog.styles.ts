import { makeStyles } from "@fluentui/react-components";

// NavSignOutDialog uses Fluent Dialog defaults — no custom overrides needed.
// This file is a placeholder satisfying the component structure convention.
export const useNavSignOutDialogStyles = makeStyles({});
